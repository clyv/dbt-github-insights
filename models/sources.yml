version: 2

# Local mode: staging models read {{ ref('raw_github_events') }} after `dbt seed`.
# BigQuery GitHub Archive can be wired here later when cloud is enabled.

sources:
  - name: local_github
    description: Sample GitHub Archive–shaped events loaded via seeds/raw_github_events.csv
    tables:
      - name: events
        identifier: raw_github_events
