version: 2

models:
  - name: stg_pr_events
    description: Staging view for PullRequestEvent rows.
    meta:
      owner: "@clyv"
      data_domain: developer_activity
      refresh_cadence: daily partition
    columns:
      - name: event_id
        tests:
          - not_null
