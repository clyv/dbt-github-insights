# PowerShell (each session), or add to your profile:
#   $env:DBT_PROFILES_DIR = (Get-Location).Path

# Only needed for the BigQuery target. The key file itself is gitignored --
# never commit it.
# $env:GCP_PROJECT_ID = "your-project-id"
# $env:GCP_SERVICE_ACCOUNT_KEY_PATH = "C:\path\to\service-account.json"

# Only needed to run the Python model on BigQuery (Dataproc Serverless).
# $env:GCP_DATAPROC_BUCKET = "your-staging-bucket"
