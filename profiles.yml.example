# Copy to profiles.yml in this repo and set DBT_PROFILES_DIR to the repo root, e.g.:
#   PowerShell: $env:DBT_PROFILES_DIR = "C:\Users\gejuj\dbt-github-insights"

github_archive:
  target: local
  outputs:
    local:
      type: duckdb
      path: data/github_archive.duckdb
      threads: 4
